apiVersion : apps/v1beta1
kind: Deployment
metadata:
  name: workplanner 
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: workplanner 
    spec:
      containers:
        - name: workplanner 
          image: webappsregistryal.azurecr.io/workplanner
          ports:
          - containerPort: 5741